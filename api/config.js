
/**
 *接口地址 
 */

const host_server = '**********************'

const regSmallUser = host_server + '/Home/Login/regSmallUser'

const getUid = host_server + '/Home/Login/getUid'

const Goodscate = host_server + '/Home/Goodscate/index'

const Ad = host_server + '/Home/Ad/index'

const getGoods = host_server + '/Home/Goods/getGoods'

const OpenGroup = host_server + '/Home/Goods/OpenGroup'

const tenant = host_server + '/Home/Goods/tenant'

const group = host_server + '/Home/Goods/group'

const getUserInfo = host_server + '/Home/User/getUserInfo'

const myGroup = host_server + '/Home/Group/myGroup'

const goinGroup = host_server + '/Home/Goods/goinGroup'

const addOrder = host_server + '/Home/Order/addOrder'

const checkOrder = host_server + '/Home/Order/checkOrder'

const getOrder = host_server + '/Home/Order/getOrder'

const orderInfo = host_server + '/Home/Order/orderInfo'

const orderList = host_server + '/Home/Order/orderList'

const calOrder = host_server + '/Home/Order/calOrder'

const getCoupon = host_server + '/Home/User/getCoupon'

const getAddress = host_server + '/Home/Address/getAddress'

const saveDefault = host_server + '/Home/Address/saveDefault'

const delAddress = host_server + '/Home/Address/delAddress'

const saveAddress = host_server + '/Home/Address/saveAddress'

const checkPwd = host_server + '/Home/Login/checkPwd'

const send = host_server + '/Home/Login/send'

const samllBind = host_server + '/Home/Login/samllBind'

const getPointGoods = host_server + '/Home/PointGoods/getPointGoods'

const OpenGroupSuccess = host_server + '/Home/Group/openGroup'

const goinGroupSuccess = host_server + '/Home/Group/goinGroup'

const loginGetUserInfo = host_server + '/Home/Login/getUserInfo'

export default{
  regSmallUser,
  getUid,
  Goodscate,
  Ad,
  getGoods,
  OpenGroup,
  tenant,
  group,
  getUserInfo,
  myGroup,
  goinGroup,
  addOrder,
  checkOrder,
  getOrder,
  orderInfo,
  orderList,
  calOrder,
  getCoupon,
  getAddress,
  saveDefault,
  delAddress,
  saveAddress,
  checkPwd,
  send,
  samllBind,
  getPointGoods,
  OpenGroupSuccess,
  goinGroupSuccess,
  loginGetUserInfo
}